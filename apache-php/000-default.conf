# Archivo: apache-php/000-default.conf (Permisos Máximos)

<VirtualHost *:80>
    DocumentRoot /var/www/html

    <Directory /var/www/html>
        # Permitimos TODAS las opciones para que el .htaccess funcione sin fallar
        # MultiViews y FollowSymLinks son CLAVE.
        Options All 
        
        AllowOverride All
        Require all granted
    </Directory>
    # ...
</VirtualHost>
